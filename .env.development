# Development auth override — no real OIDC needed
VITE_AUTH_MODE=bypass
VITE_OIDC_AUTHORITY=http://localhost:8080/realms/fusion
VITE_OIDC_CLIENT_ID=fusion-spectra

# Plugin remotes — adjust port if changed
VITE_INDEX_PLUGIN_URL=http://localhost:3001
